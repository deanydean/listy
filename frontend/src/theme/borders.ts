import { colours } from './colours';

export const borders = {
  basic: `2px solid ${colours.borderBasic}`,
  inactive: `3px solid ${colours.borderInactive}`,
  active: `4px solid ${colours.borderActive}`,
  radius: {
    s: '5px',
    m: '20px',
    l: '50%',
  },
};
