import { borders } from './borders';
import { colours } from './colours';
import { fonts } from './fonts';
import { sizes } from './sizes';

const theme = { colours, borders, fonts, sizes };

export default theme;
